// Define a simple function to greet the user
function greet(name) {
    return `Hello, ${name}!`;
  }
  
  // Export the greet function so it can be used by consumers of the package
  module.exports = {
    greet
  };
  